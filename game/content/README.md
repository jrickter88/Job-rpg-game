# Content source

Content is authored as UTF-8 JSON, one top-level record per file. Category folders
are scanned recursively by the future content loader. The exact contract and stable
ID rules are defined in `CONTENT_SCHEMA.md`.

No runtime content has been added yet. The first vertical slice should introduce a
small, validated fixture pack rather than production-scale content.

